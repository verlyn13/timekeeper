Adaptive Module
==============

Adaptive Temporal System (Definition 21)
----------------------------------------

.. automodule:: python.adaptive_agent_temporal
   :members:
   :undoc-members:
   :show-inheritance:
   :special-members: __init__

Coefficient Adjuster
--------------------

# Assuming coefficient_adjuster is part of adaptive_agent_temporal or another module
# If it's a separate file src/python/coefficient_adjuster.py use:
# .. automodule:: python.coefficient_adjuster
# If it's within adaptive_agent_temporal, document it there.
# Removing this section for now as the module likely doesn't exist standalone.

Reconfiguration
---------------

# Assuming reconfiguration is part of adaptive_agent_temporal or another module
# If it's a separate file src/python/reconfiguration.py use:
# .. automodule:: python.reconfiguration
# If it's within adaptive_agent_temporal, document it there.
# Removing this section for now as the module likely doesn't exist standalone.